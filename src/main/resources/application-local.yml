spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          public-key-location: classpath:local-public-key.pub

prisontoprobation:
  client:
    client-id: prison-to-probation-update-api-client
    client-secret: clientsecret
    access-token-uri: http://localhost:8090/auth/oauth/token
  message-processor:
    enabled: false

graceful:
  shutdown.enabled: false

hmpps.sqs:
  localstackUrl: http://localhost:4576
  dpsQueue:
    queueName: ${random.uuid}
    dlqName: ${random.uuid}
  hmppsQueue:
    queueName: ${random.uuid}
    dlqName: ${random.uuid}

oauth:
  endpoint.url: http://localhost:8090/auth

prison:
  endpoint.url: http://localhost:8093

community:
  endpoint.url: http://localhost:8096

offender-search:
  endpoint.url: http://localhost:8097

dynamodb:
  endpoint: http://localhost:4569
  tableName: cp-message
  schedule:
    tableName: cp-schedule

aws:
  provider: localstack
